package com.myapi.assertion;

public class CompleteAssertion {

    // for complete assert we use Jsonassert library.
    //we can also use Junit.
    // in order to complete assert we just copy the json file into text file. Then put on our framework folder.
    // then assign to String class and get actual result. Then use junit for assertion.
    // we have Jsonassert library for complete assert. it is very useful.

}
